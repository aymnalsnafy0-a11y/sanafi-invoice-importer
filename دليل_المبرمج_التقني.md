# دليل تقني للمبرمج — أداة استيراد الفواتير من PDF/صورة

هذا الدليل موجّه لك مباشرة كمطوّر AccSystem (الشامل بلس). فيه كل شي جرّبناه
واشتغل فعلياً على بيانات حقيقية، عشان تقدر تبني نفس الفكرة **مدمجة داخل
البرنامج نفسه** (زر بشاشة فاتورة المشتريات) بدل أداة خارجية منفصلة.

---

## 1. الهدف

المستخدم يفتح فاتورة مورد (PDF أو صورة ملتقطة بالجوال)، ويضغط زر واحد
فيتعبّى جدول بنود الفاتورة (الباركود، رقم الصنف، اسم الصنف، الوحدة،
الكمية، سعر الوحدة، الإجمالي) تلقائياً — بدل الإدخال اليدوي بند-بند.

## 2. الحل المُثبَت حالياً (نموذج أولي خارجي، Python)

كل الكود الشغّال موجود بمجلد: `D:\scanar\invoice_importer\`

```
invoice_importer/
├── app.py              # الواجهة الرسومية (Tkinter) - نقطة البداية
├── config.py            # كل الإعدادات (اتصال DB، مفتاح API، إلخ)
├── vision_extract.py     # الاستخراج بالذكاء الاصطناعي (الأهم)
├── db_items.py           # الاتصال بقاعدة Oracle وقراءة الأصناف
├── items.py              # منطق المطابقة (باركود ثم اسم)
├── ocr.py                 # محرك OCR محلي احتياطي (Tesseract) - غير مستخدم فعلياً الآن
├── exporter.py            # تصدير Excel
├── amn_exporter.py        # تصدير "ملف قياسي" (DataSet XML)
└── requirements.txt        # مكتبات Python المطلوبة
```

**للتشغيل والتجربة بنفسك:**
```
pip install -r requirements.txt
python app.py
```
يحتاج Python 3.10+ (جُرِّب فعلياً على 3.14). يحتاج مفتاح Anthropic API
(في ملف `anthropic_api_key.txt` بنفس المجلد — اسأل المستخدم يعطيك نسخة أو
اعمل حساب جديد بـ console.anthropic.com).

---

## 3. الاستخراج بالذكاء الاصطناعي (الجزء الأهم)

### لماذا مو OCR تقليدي؟

جرّبنا Tesseract (محلي، مجاني) أول شي. فشل بشكل ثابت على فواتير حقيقية
ملتقطة بالجوال: كان يخلط أرقام عمود "رقم الصنف الداخلي" أو الباركود مع
عمود السعر لأنه يعتمد على **موقع البكسل** فقط، بدون فهم حقيقي لمعنى كل
عمود. حتى بعد محاولات تحسين متعددة (استبعاد الأرقام الطويلة كباركود،
ربط موقع رأس العمود، إعادة تجميع الأسطر حسب الإحداثي الرأسي)، ظل يخطئ في
فواتير معقدة (صنف واحد مطبوع على سطرين، خلفية ملوّنة لصف العناوين...).

### الحل: Claude Vision (Anthropic API)

نرسل صورة الفاتورة كاملة، مع طلب **structured output** (Pydantic model)
يرجع مصفوفة JSON منظمة. الموديل "يشوف" الجدول فعلياً ويفهم أي عمود هو
الكمية وأي عمود السعر بالمعنى، مو بالموقع — فحل المشكلة نهائياً.

**النتيجة الفعلية بالاختبار**: 100% دقة (11/11 صنف، ثم 14/14 صنف) على
فاتورتين حقيقيتين مختلفتين، شاملة الباركود والاسم والسعر والإجمالي.

### الكود (من `vision_extract.py`)

```python
import anthropic
from pydantic import BaseModel

class VisionLineItem(BaseModel):
    description: str
    barcode: str | None = None
    unit: str | None = None
    quantity: float | None = None
    unit_price: float | None = None
    total: float | None = None

class VisionExtraction(BaseModel):
    items: list[VisionLineItem]

client = anthropic.Anthropic(api_key=API_KEY)
response = client.messages.parse(
    model="claude-opus-5",
    max_tokens=8000,
    output_config={"effort": "high"},
    messages=[{
        "role": "user",
        "content": [
            {"type": "image", "source": {"type": "base64", "media_type": "image/png", "data": image_b64}},
            {"type": "text", "text": EXTRACTION_PROMPT},
        ],
    }],
    output_format=VisionExtraction,
)
items = response.parsed_output.items  # قائمة بنود جاهزة، منظمة، بدون parsing يدوي
```

نص الطلب الكامل (`EXTRACTION_PROMPT`) موجود بنفس الملف — يشرح للموديل:
تجاهل رأس/تذييل الفاتورة وأسطر الضريبة، اعتبر الصنف المطبوع على سطرين
صنفاً واحداً، فضّل الاسم العربي على الإنجليزي لو الاثنين موجودين، اجعل أي
حقل غير واثق منه `null` بدل التخمين.

### التكلفة

قِسناها فعلياً (مو تقدير): فاتورة ١١ صنف ≈ **0.0335$** (input: 2858 توكن،
output: 768 توكن). يعني ~150 فاتورة بـ 5 دولار. رخيصة جداً مقابل الفايدة.

---

## 4. قاعدة البيانات — القراءة والمطابقة

### الاتصال

Oracle محلي، `localhost:1521/orcl` (EZConnect، بدون حاجة لملف
tnsnames.ora مع python-oracledb في وضع thin). بيانات الدخول موجودة
بـ `config.py` عندك — نفسها لكل العملاء حسب ما أكدت لنا.

### اكتشاف اسم الشركة (schema) تلقائياً

```sql
SELECT username FROM all_users WHERE username LIKE 'SAL%' ORDER BY created DESC
```
نمط الاسم: `SAL_<سنة>_<رقم>` (نستثني `SAL_MANG` الإداري بالكود). لو أكثر
من نتيجة مطابقة، نخيّر المستخدم بدل التخمين — بعض العملاء عندهم أكثر من
"ملف" نشط بنفس الوقت.

### ⚠️ الاكتشاف الأهم بكل المشروع: مكان الباركود الحقيقي

عمود `CLASSES.GTIN` **فاضي لمعظم الأصناف تقريباً**. الباركود الفعلي
المطبوع على الفواتير ويستخدمه المستخدمون يومياً مخزّن بجدول منفصل:

```sql
SELECT c.CLS_ID, c.CLS_NO, c.CLS_ARNAME, c.CLS_ENNAME, c.CLS_UN_1,
       b.UN_BARCODE_NO, b.UN_ID
FROM CLASSES c
LEFT JOIN CLS_UNIT_BARCODE b ON b.CLS_ID = c.CLS_ID
```

`CLS_UNIT_BARCODE` يربط كل صنف (`CLS_ID`) بباركود لكل وحدة قياس (`UN_ID`)
— صنف واحد ممكن يكون له أكثر من باركود (وحدة مفردة، كرتون...). لو تجاهلت
هذا الجدول واعتمدت GTIN بس، أي مطابقة بالباركود بتفشل بصمت حتى لو انقرا
صح من الصورة — بالضبط الخطأ اللي وقعنا فيه أول مرة واكتشفناه بمقارنة
النتيجة مع شاشة فاتورة حقيقية.

### منطق المطابقة (من `items.py`)

1. **أولوية للباركود**: طابق `barcode` المستخرج من الصورة مباشرة مع
   `UN_BARCODE_NO` بجدول الأصناف — تطابق دقيق 100%، مافي تخمين.
2. **احتياطي: تقارب الاسم** (fuzzy matching، rapidfuzz أو أي مكتبة
   تقارب نصي مشابهة) — يُستخدم بس لو ما فيه تطابق باركود.
3. **لو ما فيه تطابق واثق (تحت عتبة معينة)**: **لا تخمّن رقم صنف**. اترك
   `رقم الصنف` فاضي واعرض اسم/سعر/كمية الصنف كما انقرا من الفاتورة —
   قرار مستخدم صريح: "ما يهمني إذا الصنف موجود عندي أو لا، أبي البيانات
   تنزل كما هي بأماكنها الصحيحة". عالج الصف كتنبيه (يحتاج إضافة صنف جديد
   أو مراجعة يدوية)، مو كخطأ.
4. **الوحدة**: أغلب الفواتير ما تطبع عمود وحدة. لو الصنف انطابق، عبّي
   الوحدة تلقائياً من `CLASSES.CLS_UN_1` (الوحدة الافتراضية المسجَّلة
   للصنف) بدل تركها فاضية.

---

## 5. مخطط مقترح للدمج المباشر داخل الشاشة

بدل تصدير ملف واستيراده يدوياً (الحل الحالي بالأداة الخارجية)، الأنسب
عندك (بما إن عندك وصول مباشر لشبكة الجدول بالشاشة):

1. زر جديد بشاشة فاتورة المشتريات: "استيراد من PDF/صورة" (جنب الأزرار
   الموجودة أصلاً "استيراد من اكسل"/"استيراد من ملف قياسي").
2. Dialog لاختيار الملف (PDF/صورة) → استدعاء Claude API بنفس الطريقة
   أعلاه → تحويل إلى نفس شكل الجدول الداخلي (list of rows).
3. مطابقة كل صف مباشرة مع قاعدة بياناتك الحية (نفس الاستعلامات أعلاه) —
   هذا أسهل عندك من عندنا لأنك أصلاً متصل بقاعدة البيانات من داخل التطبيق.
4. تعبئة صفوف الجدول (grid) مباشرة بالنتيجة، بدل ملف وسيط — وفّر خطوة
   تصدير/استيراد كاملة، وخلي المستخدم يراجع/يعدّل مباشرة بنفس شاشة
   الفاتورة العادية (مو شاشة منفصلة).
5. (اختياري) تلوين الصفوف: أخضر = صنف مطابَق، كهرماني = صنف غير موجود
   بالقاعدة (يحتاج مراجعة، مو خطأ).

هذا الحل أنظف بكثير من حلّنا الخارجي — ما يحتاج ملفات وسيطة (اكسل/ملف
قياسي) ولا نافذة منفصلة، لأنك تقدر تكتب مباشرة بجدول الفاتورة المفتوح.

---

## 6. مراجع الصيغ (لو احتجتها لأي سبب)

- **قالب استيراد اكسل الحقيقي**: `aman_import_template.xlsx` بمجلد
  الأداة — أعمدته بالترتيب: الباركود، رقم الصنف، اسم الصنف، الوحدة،
  الكمية، سعر الوحدة، الإجمالي.
- **صيغة "ملف قياسي"**: XML من نوع .NET DataSet، جدولين `DOC_MST_TABLE`
  (رأس الفاتورة) و`DOC_DET_TABLE` (بنود، يتكرر لكل صنف). التفاصيل
  الكاملة وأسماء كل الحقول موجودة بـ `amn_exporter.py` (نسخة من صيغة
  حقيقية فحصناها من تصدير فعلي من داخل الشامل).

## 7. قيود/تحديات معروفة بالحل الحالي

- المطابقة بالاسم (fuzzy) ليست مضمونة 100% — دايماً فضّل الباركود.
- الذكاء الاصطناعي يحتاج اتصال إنترنت، وله تكلفة (بسيطة جداً لكن موجودة).
- ملف الإعدادات `AccSystem.exe.config` عنده بيانات اتصال وهمية دايماً
  (`oracle_user`/`oracle_user_password`) — لا تعتمد عليه لاكتشاف بيانات
  الاتصال الحقيقية، استخدم القيم الثابتة المؤكدة (نفسها بكل العملاء حسب
  تأكيدكم) بدلاً منه مباشرة.
