# دليل أداة "سنافي" — شرح كامل + أسئلة متوقعة من المبرمج

هذا الملف لك أنت فقط (محفوظ محلياً على جهازك، مو منشور لأي حد). اقرأه كامل
قبل المكالمة، وارجع له وقت اللزوم أثناء الشرح.

---

## 1. الفكرة بجملة وحدة

الأداة تفتح فاتورة مشتريات (PDF أو صورة)، تقرأ الأصناف والكميات والأسعار
منها تلقائياً، تطابقها مع أصناف موجودة فعلاً بنظام أمان الشامل، وتصدّر ملف
(اكسل أو ملف قياسي) تستورده بنفس الزر الموجود أصلاً بشاشة فاتورة
المشتريات. الأداة **لا تلمس ولا تعدّل** برنامج الشامل نفسه إطلاقاً — بس
تجهّز له ملف يفهمه.

## 2. ليش أداة منفصلة، وليش ما عدّلنا برنامج الشامل نفسه؟

- برنامج AccSystem.exe (الشامل بلس) برنامج تجاري **مغلق المصدر** — ما فيه
  كود مصدري متاح للتعديل، وحتى لو كان متاح، تعديل برنامج محاسبي حي فيه
  مخاطرة حقيقية (منطق حساب المخزون، القيود المحاسبية، فاتورة الإلكترونية
  كلها مبرمجة جوا الملف المصرّف، ما نقدر نتأكد نعيد كتابتها صح).
- الحل الأذكى والأأمن: نخلي الشامل نفسه يسوي الاستيراد الفعلي (هو مبرمج
  صح ومجرّب من الشركة المطوّرة)، وإحنا بس نجهّز له البيانات بالشكل اللي
  يفهمه (نفس آلية "استيراد و تصدير" الموجودة أصلاً بالبرنامج).

## 3. خطوات عمل الأداة بالترتيب

1. **فتح البرنامج** → يتصل تلقائياً بقاعدة بيانات الشامل (Oracle) ويجيب
   قائمة الأصناف الحية (رقم الصنف، الاسم، الباركود، الوحدة الافتراضية).
2. **اختيار ملف الفاتورة** (PDF أو صورة/لقطة جوال).
3. **الاستخراج**: الصورة تُرسل لخدمة ذكاء اصطناعي بصري (Claude من شركة
   Anthropic) تقرأ الجدول وتطلع لكل صنف: الوصف، الباركود، الوحدة، الكمية،
   السعر، الإجمالي — بفهم حقيقي لشكل الجدول، مو مجرد "قراءة حروف" عشوائية.
4. **المطابقة التلقائية**: كل صنف مستخرج يتقارن مع قائمة أصناف الشامل:
   - أول شي: مطابقة بالباركود (دقيقة 100% إذا الباركود موجود ومطابق).
   - إذا ما فيه باركود مطابق: مطابقة بالاسم (تقارب نصي ذكي، rapidfuzz).
   - لو ما لقى تطابق واثق: يسيب البيانات المقروءة كما هي (بدون رقم صنف)
     بدل ما يحط رقم غلط — ويلوّن الصف كهرماني (تنبيه، مو خطأ).
5. **مراجعة يدوية**: جدول تقدر تعدّل فيه أي خلية بنقرة مزدوجة قبل التصدير.
6. **التصدير**: زرين — اكسل، أو "ملف قياسي" (نفس صيغة AccSystem الداخلية).
7. **الاستيراد الفعلي**: يصير من داخل الشامل نفسه، بنفس الزر الموجود أصلاً
   بشاشة فاتورة المشتريات ("استيراد و تصدير").

## 4. التقنيات المستخدمة (لو سألك عن الـ"stack")

- **Python** لغة البرمجة، **Tkinter** للواجهة الرسومية.
- **PyMuPDF (fitz)**: تحويل صفحات PDF لصور.
- **Anthropic API (Claude Vision)**: قراءة/فهم محتوى الفاتورة.
- **python-oracledb**: الاتصال المباشر بقاعدة بيانات Oracle الخاصة بالشامل
  (بدون الحاجة لتثبيت Oracle Client كامل — يعمل بوضع "thin mode").
- **openpyxl**: قراءة/كتابة ملفات اكسل.
- **rapidfuzz**: خوارزمية تقارب نصي (fuzzy matching) للمطابقة بالاسم.

## 5. تفاصيل قاعدة البيانات (مهم لو سأل تفاصيل تقنية)

- الأداة تتصل مباشرة بقاعدة Oracle المحلية اللي يستخدمها الشامل نفسه
  (`localhost:1521/orcl`)، بحساب له صلاحية قراءة كاملة.
- **قراءة فقط (Read-Only)** — الأداة ما تكتب ولا تعدّل أي شي بقاعدة
  البيانات إطلاقاً، فقط SELECT.
- تكتشف تلقائياً اسم "الشركة/الملف" (schema) — نمط `SAL_سنة_رقم` — بدون
  الحاجة تكتب الاسم يدوياً، فتنتقل لأي عميل ثاني عنده نفس الإعداد بدون
  تعديل بالكود. لو فيه أكثر من ملف نشط بنفس الوقت، تسأل المستخدم يختار.
- الباركود الحقيقي يُقرأ من جدول `CLS_UNIT_BARCODE` (مربوط بجدول
  `CLASSES` عبر `CLS_ID`) — مو من عمود `GTIN` (يطلع فاضي غالباً).

## 6. أسئلة متوقعة من المبرمج + الجواب الجاهز

**"كيف تقرأ الصورة؟ أي مكتبة OCR تستخدم؟"**
> جربت أول شي OCR محلي (Tesseract)، بس دقته ضعيفة على صور الجوال المزدحمة
> (يخلط بين الأعمدة). تحولت لخدمة ذكاء اصطناعي بصري (Claude Vision عبر
> API) لأنها تفهم تركيب الجدول فعلياً مو بس تجمع حروف، والدقة صارت 100%
> على الفواتير اللي جربتها.

**"وش لو الصنف مو موجود بالنظام؟"**
> يطلع الصف بلون كهرماني (تنبيه، مو خطأ) والبيانات المقروءة (الاسم،
> السعر، الكمية) تبقى صحيحة زي ما هي، بس بدون رقم صنف — عشان ما نحط رقم
> غلط. المستخدم يقدر يضيف الصنف بالشامل أول أو يكتب الرقم يدوياً.

**"هل الأداة تكتب أي شي بقاعدة البيانات؟"**
> لا، قراءة فقط (SELECT). كل الكتابة الفعلية تصير جوا الشامل نفسه عبر زر
> الاستيراد الموجود أصلاً — الأداة بس تجهّز الملف.

**"كيف تتأكد الملف المُصدَّر يطابق صيغة الشامل؟"**
> فحصت ملف حقيقي مُصدَّر من داخل الشامل نفسه (كل من الاكسل والملف القياسي)
> وبنيت التصدير بالضبط على نفس بنيته وأسماء أعمدته/حقوله.

**"لو نقلناها لعميل ثاني، بتشتغل؟"**
> نعم، طالما نفس إعداد قاعدة البيانات المحلية (Oracle على نفس الجهاز،
> نفس نمط كلمة المرور). اسم الشركة يُكتشف تلقائياً، ما يحتاج تعديل كود.

**"وش تكلفة تشغيلها؟"**
> فاتورة متوسطة (~11 صنف) تكلف تقريباً 3 هللات (أقل من نص ريال بكثير) —
> مقاسة فعلياً، مو تقدير. أرخص بكثير من وقت الإدخال اليدوي.

**"ليش ما استخدمت مكتبة X بدل Y؟"** (إن سأل عن تفاصيل برمجية عميقة)
> كانت أبسط وأسرع حل موثوق لكل مشكلة على حدة أثناء التطوير والاختبار على
> فواتير حقيقية — كل قرار كان مبني على تجربة فعلية مو افتراض نظري.

## 7. للمبرمج تحديداً: مخطط جاهز للدمج المباشر داخل البرنامج

بما إنك المطوّر الفعلي وعندك الكود المصدري، الأفضل من الأداة المنفصلة إنك
تضيف زر داخل شاشة فاتورة المشتريات نفسها ("استيراد من PDF/صورة") يعبّي
الصفوف مباشرة بالجدول، بدون تصدير/استيراد يدوي. هذا ملخص كل شي جرّبناه
واشتغل فعلياً 100% على فواتير حقيقية، تقدر تبني عليه مباشرة:

### أ) قراءة الفاتورة (الاستخراج)
- جرّبنا OCR محلي (Tesseract) أول شي — فشل على صور الجوال (يخلط بين
  الأعمدة، يقرأ رقم الصنف الداخلي أو الباركود كأنه سعر).
- **الحل اللي نجح 100%**: إرسال صورة الفاتورة إلى Claude API (Anthropic)
  مع طلب structured output (JSON) يحدد لكل صنف: الوصف، الباركود، الوحدة،
  الكمية، سعر الوحدة، الإجمالي. الموديل يفهم تركيب الجدول فعلياً (يميّز
  عمود عن عمود بالمعنى، مو بموقع البكسل)، فيحل مشكلة اختلاط الأعمدة نهائياً.
- تفاصيل تقنية: `client.messages.parse()` مع `output_format` (Pydantic
  model)، الموديل `claude-opus-5`. التكلفة قليلة جداً (~0.03$ لفاتورة ١١
  صنف، حسبناها فعلياً من استخدام حقيقي).
- ملف مرجعي بالكود الحالي: `vision_extract.py` (فيه نص الطلب الكامل
  المُجرَّب وبنية الـ JSON المطلوبة).

### ب) مطابقة الصنف مع قاعدة البيانات (الأهم)
- **لا تعتمد على مطابقة الاسم فقط** — جرّبناها ففشلت حتى لأصناف موجودة
  فعلاً، لأن صياغة الاسم بالفاتورة تختلف شوي عن قاعدة البيانات.
- **الصح**: مطابقة بالباركود أولاً (دقيقة 100%). **تنبيه مهم**: الباركود
  الحقيقي المستخدم فعلياً على الفواتير مخزّن بجدول `CLS_UNIT_BARCODE`
  (مربوط بـ `CLASSES` عبر `CLS_ID`)، **وليس** عمود `GTIN` بجدول `CLASSES`
  (يطلع فاضي لمعظم الأصناف). هذا أهم اكتشاف تقني بكل المشروع — لو تجاهلته
  المطابقة تفشل بصمت حتى لو الباركود انقرا صح من الصورة.
- استعلام مرجعي جاهز (Oracle):
  ```sql
  SELECT c.CLS_ID, c.CLS_NO, c.CLS_ARNAME, c.CLS_ENNAME, c.CLS_UN_1,
         b.UN_BARCODE_NO, b.UN_ID
  FROM CLASSES c
  LEFT JOIN CLS_UNIT_BARCODE b ON b.CLS_ID = c.CLS_ID
  ```
- لو ما فيه باركود مطابق: مطابقة احتياطية بتقارب الاسم (fuzzy matching،
  مكتبة rapidfuzz بكودنا، أي مكتبة تقارب نصي مشابهة تكفي عندك).
- لو ما فيه تطابق واثق: **لا تخمّن رقم صنف** — اترك الحقل فاضي واعرض
  البيانات المقروءة كما هي، ولوّن الصف تنبيهياً (مو أحمر/خطأ، بس تنبيه).
- **الوحدة**: أغلب الفواتير ما تطبع عمود وحدة أصلاً. لو الصنف انطابق،
  عبّي الوحدة تلقائياً من `CLASSES.CLS_UN_1` (الوحدة الافتراضية للصنف).

### ج) اكتشاف اسم الشركة (schema) تلقائياً
- نمط التسمية: `SAL_<سنة>_<رقم>` (يستثني `SAL_MANG` الإداري). استعلام:
  `SELECT username FROM all_users WHERE username LIKE 'SAL%'`.
  لو أكثر من نتيجة، خيّر المستخدم بدل التخمين (بعض العملاء عندهم أكثر من
  ملف نشط بنفس الوقت).

### د) الكود الكامل للرجوع له
كل هذا مُطبَّق وشغّال بمجلد `D:\scanar\invoice_importer\` — أهم ملفين:
`vision_extract.py` (الاستخراج) و`items.py` (منطق المطابقة). باقي الملفات
(exporter.py، amn_exporter.py) مو محتاجها لو راح يدمجها مباشرة بالجدول
بدل التصدير لملف، بس فيها مرجع لصيغة "ملف قياسي" لو احتاجها لأي غرض ثاني.

## 8. حدود معروفة (لو سأل "وش الأداة ما تسويه")

- تحتاج اتصال إنترنت (لخدمة الذكاء الاصطناعي) واشتراك مدفوع بسيط التكلفة.
- المطابقة بالاسم (لما ما فيه باركود) تقارب ذكي مو مضمون 100%، فمراجعة
  الصفوف الكهرمانية يدوياً خطوة مهمة قبل التصدير.
- صيغة "ملف قياسي" تستبعد تلقائياً أي صنف بدون رقم صنف مؤكد (بعكس الاكسل
  اللي يقبل صف بدون رقم صنف).
