# أسرار وبيانات اتصال محلية - خاصة بكل جهاز، أبداً ما تُرفع
anthropic_api_key.txt
oracle_credentials.txt
server_address.txt

# حالة تشغيل محلية (تُنشأ تلقائياً، مختلفة لكل جهاز/مستخدم)
usage_state.json
learned_matches.json
matching_settings.json
semantic_rerank_cache.json

# ملفات بايثون مؤقتة
__pycache__/
*.pyc

# نواتج اختبار/تصدير محلية (لو وُجدت)
*.egg-info/
.pytest_cache/

# مدخلات Benchmark حقيقية (فاتورة/تسعير/كتالوج صنف فعلي للعميل) + نواتجه -
# بيانات عمل حقيقية، أبداً ما تُرفع
test_invoices/

# نواتج مختبر Promptfoo المحلية (بيانات فواتير حقيقية مُستخرَجة + نتائج
# تشغيل) - مو كود، تُنشأ محلياً بـpromptfoo/generate_dataset.py
promptfoo/tests.json
promptfoo/results.json
promptfoo/node_modules/

