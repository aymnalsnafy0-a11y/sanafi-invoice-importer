PyMuPDF>=1.23
Pillow>=10.0
rapidfuzz>=3.5
openpyxl>=3.1
anthropic>=0.40
pydantic>=2.0
oracledb>=2.0

# اختياري: فقط لو بدّلت EXTRACTION_BACKEND في config.py إلى "tesseract"
# (الوضع الحالي "vision" لا يحتاجها إطلاقاً، وحذفها يقلل حجم التثبيت كثيراً)
# opencv-python>=4.8
# pytesseract>=0.3.10
